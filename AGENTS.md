be consice; omit grammer

## Working agreements
- Prefer `bun` when installing dependencies.
- Always run `bun run lint` after modifying JavaScript/Typescript files. Fix any issues that arise
- Always run `bun run test` after modifying JavaScript/Typescript files. Fix any issues that arise
- Always run `bun run convex dev --once` after modifying Convex logic. Such as schemas, queries and mutations
- Ask for confirmation before adding new production dependencies.


Read /UBIQUITOUS_LANGUAGE.md to common terms that are used to communicate
