# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "Copy env, and bun install"

[setup]
script = '''
#!/bin/bash
# Generic setup script
# Available variables: $MAIN_CHECKOUT, $WORKTREE_PATH, $BRANCH_NAME
MAIN_CHECKOUT=/Users/abeldebruijn/Documents/GitHub/prompt-party-portal

# Copy environment and config files
for file in .env .env.local .envrc .tool-versions; do
  if [ -f "$MAIN_CHECKOUT/$file" ]; then
    cp "$MAIN_CHECKOUT/$file" "./$file"
    echo "Copied $file"
  fi
done

bun i
'''
